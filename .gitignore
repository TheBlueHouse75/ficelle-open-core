# Runtime state
catalog.json
config.json
state.json
logs/
.ficelle-evals/

# Python build/cache
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
.pytest_cache/
.coverage
# Local uv artifact: CI installs with `pip install -e '.[dev]'`, so nothing reads
# this lock. Track it only if the workflows move to `uv sync --frozen`.
uv.lock

# Local/editor
.DS_Store
*.log

# MCP tool scratch output
.playwright-mcp/
